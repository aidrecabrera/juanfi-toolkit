{
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "typecheck": "tsc -b packages/api packages/hotspot packages/admin"
  },
  "devDependencies": {
    "tsdown": "^0.22.0",
    "typescript": "latest",
    "vitest": "latest"
  },
  "version": "0.0.1"
}
